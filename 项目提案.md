# 学习智能助手（StudyApp）项目提案

> 一个打包成 exe 的桌面学习助手：**输入课程名 → 自动生成 课程大纲 + 推荐资源 + 学习计划**。

---

## 评分维度自查

### 1. 📋 功能完整性（30 分）——能做什么

**Agent 核心特征**（评分重点：是否体现 Agent 特征）
- Agent Loop：OBSERVE → THINK → ACT → EVALUATE 循环执行
- 意图识别：听懂输入 → 决定调哪个 Agent
- 工具调用：知识库 / 资源搜索 / LLM 生成，按需调用
- 多 Agent 协作：Planner / Searcher / Writer / Reviewer 分工 + 质检修订

**工具数量与功能**（都跑通）

| # | 功能 | 说明 |
|---|------|------|
| 1 | 生成学习方案 | 输入课程 → 三栏输出：大纲 + 资源 + 计划（核心） |
| 2 | 课程问答 | 知识库检索 + DeepSeek 回答 |
| 3 | 知识库管理 | 导入 / 搜索 / 删除资料（JSON 持久化） |
| 4 | 日志回放 | 每次执行可回放 trace，出问题能定位 |

### 2. 🏗️ 代码质量（25 分）——怎么写

- **结构规范**：分层目录（agents / tools / core / ui），单一职责
- **命名清晰**：planner / searcher / writer / reviewer，见名知义
- **中文注释**：每个模块 docstring 说明职责，关键逻辑有注释
- **测试覆盖**：pytest 单元测试 + 20 道固定题 Eval（正确率 ≥80%）
- **日志完整**：Agent 每步 trace 落盘（jsonl）+ 回放器

### 3. 💡 创意度（15 分）——为什么值得做

- **解决真实问题**：大学生排学习计划全靠手工，输入课程名一次给全方案，省时
- **思路巧妙**：三个 Agent 分工（规划/搜索/写作）+ 质检修订，不是单次调 API 糊弄
- **有积累**：把前五周学的工程化能力（配置、日志、测试、多 Agent）全部整合进一个产品

### 4. 📖 README 与文档（10 分）——别人能否 clone 跑起来

- README：功能说明 + 环境要求 + 安装步骤（pip install）+ 配置 .env + 运行方式
- **克隆即可跑**：requirements.txt 装依赖 → 填 DeepSeek key → python app.py
- 打包说明：pyinstaller 一条命令出 exe
- 含演示截图 / 示例输出

### 5. 🎤 展示效果（20 分）——怎么讲

- **讲解主线**：痛点 → 核心流程（输入课程 → 三 Agent）→ 现场演示 → 亮点总结
- **Demo 脚本**：固定用 1-2 个课程名（如"机器学习"），确保流畅不断
- **准备 Q&A**：为什么用多 Agent？日志怎么回放？测试怎么做的？key 怎么保密？
- **预演**：打包好的 exe 在演示机器上提前跑通

---

## 技术方案

- 语言：Python 3.13
- 界面：tkinter（自带，无需安装）
- 大模型：DeepSeek API（key 走 .env，零硬编码）
- 打包：pyinstaller → 单文件 exe
- 数据：JSON 文件（知识库 / 日志 / 缓存）

## 代码架构

```
studybuddy/
├── app.py                 # 入口：启动窗口
├── ui/
│   ├── main_window.py     # 主窗口：输入课程 → 三栏展示
│   └── widgets.py         # 结果卡片、状态提示
├── agents/
│   ├── planner.py         # 课程 → 大纲
│   ├── searcher.py        # 课程 → 资源列表
│   ├── writer.py          # 大纲 → 学习计划
│   └── reviewer.py        # 质量检查，不过打回修订
├── tools/
│   ├── knowledge_store.py # 知识库存取
│   └── resource_search.py # 资源搜索
├── core/
│   ├── agent_loop.py      # Agent 循环 + trace + 回放
│   ├── intent.py          # 意图识别
│   ├── config.py          # dev/prod/test 配置
│   └── llm.py             # DeepSeek 封装
├── tests/                 # pytest + 20 题 Eval
├── README.md
├── build.bat              # 打包脚本
├── .env                   # 密钥
└── requirements.txt
```

## 开发安排（按分值权重配时间）

| 阶段 | 内容 | 对应分值 |
|------|------|---------|
| 1 | agents/ 四角色写通，核心流程跑通 | 功能完整性 30 |
| 2 | 界面 + 三栏展示 | 功能完整性 30 + 展示 20 |
| 3 | 测试补齐 + 日志完善 + 结构打磨 | 代码质量 25 |
| 4 | README + 打包 exe | 文档 10 + 展示 20 |
| 5 | 预演 + 准备 Q&A 话术 | 展示 20 |

